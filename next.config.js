/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for deployment flexibility
  // output: 'export',
  
  // Image optimization
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
